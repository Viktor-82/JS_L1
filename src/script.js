'use strict';

let arr = ['hello', 'world', '!'];

/* Метод pop() удаляет последний элемент из массива 
и возвращает его значение
 */
let last = arr.pop();
console.log(last);// '!'
console.log(arr);// ['hello', 'world']